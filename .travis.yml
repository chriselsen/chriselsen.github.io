language: ruby
env:
  global:
    - NOKOGIRI_USE_SYSTEM_LIBRARIES=true  
install: 
  - gem install jekyll
  - gem install html-proofer
script: 
  - jekyll build --config _config.yml
  - htmlproofer ./_site --url-ignore "/www.linkedin.com/"
